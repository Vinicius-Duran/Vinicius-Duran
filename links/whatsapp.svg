<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 262 62" width="262" height="62"
     role="img" aria-label="whatsapp: (48) 99211-0831">
  <title>whatsapp — (48) 99211-0831</title>

  <!-- Gerado por scripts/cards.mjs. Não editar à mão. -->

  <defs>
    <style><![CDATA[
      .display { font-family: 'Archivo','Helvetica Neue',Helvetica,Arial,sans-serif; font-weight: 800; }
      .body    { font-family: 'Archivo','Helvetica Neue',Helvetica,Arial,sans-serif; font-weight: 500; }
      .mono    { font-family: 'JetBrains Mono',ui-monospace,SFMono-Regular,Menlo,monospace; font-weight: 500; }
      .anim { transform-box: fill-box; }

      /* A seta corre até a borda e volta: o botão diz que leva a algum lugar. */
      .seta { animation: seta 3.4s cubic-bezier(0.23,1,0.32,1) infinite; }
      @keyframes seta {
        0%,60% { transform: translateX(0); opacity: .55; }
        75%    { transform: translateX(6px); opacity: 1; }
        100%   { transform: translateX(0); opacity: .55; }
      }
      .risco { transform-origin: left center; animation: risco 3.4s cubic-bezier(0.23,1,0.32,1) infinite; }
      @keyframes risco {
        0%,60% { transform: scaleX(.28); }
        80%    { transform: scaleX(1); }
        100%   { transform: scaleX(.28); }
      }

      @media (prefers-reduced-motion: reduce) {
        .seta, .risco { animation: none; transform: none; opacity: 1; }
      }
    ]]></style>
  </defs>

  <rect width="262" height="62" rx="12" fill="#14100c"/>
  <rect width="262" height="62" rx="12" fill="#221c18" opacity=".55"/>
  <rect x="1" y="1" width="260" height="60" rx="11" fill="#efe8dc" fill-opacity=".03" stroke="#efe8dc" stroke-opacity=".18"/>

  <text class="mono" x="20" y="26" font-size="10.5" fill="#d9743f" letter-spacing="1.6">whatsapp</text>
  <text class="body" x="20" y="46" font-size="14" fill="#efe8dc" fill-opacity=".9">(48) 99211-0831</text>
  <rect class="anim risco" x="20" y="52" width="192" height="1.5" fill="#d9743f" fill-opacity=".45"/>

  <g class="anim seta">
    <path d="M228 31 h12 m-5 -5 l5 5 l-5 5" fill="none" stroke="#d9743f" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
  </g>
</svg>
